apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: myapp-

resources:
  - ../../base

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: app
  - path: ingress-patch.yaml
    target:
      kind: Ingress
      name: app
